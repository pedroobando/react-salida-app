import React from 'react';
import ReactDOM from 'react-dom';
import { SalidaApp } from './SalidaApp';

ReactDOM.render(
  <React.StrictMode>
    <SalidaApp />
  </React.StrictMode>,
  document.getElementById('root')
);
